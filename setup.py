from setuptools import setup

setup(name='netperftest',
      version='0.0.1',
      description=('A tiny script that measures latency, and upload and '
                   'download speeds for a specified destination'),
      author='humblre',
      packages=['netperftest'])